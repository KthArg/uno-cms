# Pendientes

Todo lo que se ha aplazado, con su motivo y **su issue**. Nada de lo que hay aquí vive solo en
un comentario de código o en una conversación de un PR.

## Por qué existe este documento

Un proyecto que avanza por hitos aplaza cosas continuamente, y casi siempre con razón. El
problema no es aplazar: es que lo aplazado se disuelva. Una limitación explicada con detalle
dentro de una función es invisible desde fuera; una decisión tomada en la revisión de un PR
deja de existir en cuanto ese PR se mergea.

Este registro le da un sitio único a cada cosa, y hay un test —
`tests/unit/pendientes.test.ts` — que **falla si aparece en el código una nota de algo
aplazado sin un issue al que apuntar**. Sin ese test, el documento se quedaría atrás en dos
semanas.

## Cómo se escribe una nota aplazada

En el código, citando el issue:

```ts
// PENDIENTE(#117): sin medir. La estructura de §8 se cumple, pero un presupuesto sin
// medición es una hipótesis.
```

El test acepta `PENDIENTE`, `TODO` y `FIXME`, y exige el `#N` en la misma línea. Si no hay
issue, es que la decisión no está tomada — y entonces lo que falta no es un comentario, es
abrir el issue.

---

## Deuda viva

Ordenada por cuándo se resuelve. Lo que está en un hito ya planificado no se repite aquí salvo
que sea una limitación conocida y no una función por construir.

### En M4

| Qué                                                      | Por qué se aplazó                                                                                                                                           | Issue                                                |
| -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| La pantalla de una colección                             | El dashboard **ya enlaza** a `/admin/collections/[key]` y las actions existen desde M3. Encontrado auditando lo aplazado: ningún issue la construía         | [#111](https://github.com/KthArg/uno-cms/issues/111) |
| Nada impide llamar a código de cliente desde el servidor | Ha pasado dos veces en M4, y las dos con `typecheck`, `lint` y `build` en verde. Hace falta la frontera al revés que `server-only`                          | [#125](https://github.com/KthArg/uno-cms/issues/125) |
| Las rutas de `/api` no tienen inventario de acceso       | Los guards de #70 cubren `/admin`; una ruta bajo `/api` se protege sola y nada dice cuáles deben ser públicas. Se cierra con la primera ruta de API privada | [#104](https://github.com/KthArg/uno-cms/issues/104) |

### En M6

| Qué                                                            | Por qué se aplazó                                                                                                                                                                                                                                                                                                                                                | Issue                                                |
| -------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| El driver de producción nunca ha hablado con Neon              | Los 190 tests de integración corren contra Postgres local con `node-postgres`: lo verificado es el SQL, no el driver                                                                                                                                                                                                                                             | [#43](https://github.com/KthArg/uno-cms/issues/43)   |
| El límite de peticiones es **por instancia**                   | Cerrado en M6 como limitación conocida (#65): implementarlo sin una instancia contra la que ejercitarlo sería añadir código de seguridad sin un test que lo ejecute, y un límite mal integrado deja el sistema **pareciendo protegido**. El bloqueo de cuenta sí es global y es la defensa que sostiene el caso. Está en `docs/SECURITY.md` con cómo se cerraría | —                                                    |
| `publishAll` sigue con un tope de 100 por llamada              | Resuelto en ADR-600: el tope se queda —subirlo solo mueve el problema— y el botón encadena las vueltas desde el cliente, acumulando el informe. Lo que queda vivo es que **depende de que la pestaña esté abierta**: cerrarla a mitad deja el sitio publicado a medias, sin perder nada. La alternativa era una cola, que §2 no contempla                        | —                                                    |
| La pantalla de una colección no pagina ni avisa de los topes   | `reorderItems` acepta 500 y `publishAll` publica 100 por llamada. Con listas de una landing —cinco o diez— sobra; construir paginación para un problema que no existe es peor. Se notará porque el tope se alcance                                                                                                                                               | [#119](https://github.com/KthArg/uno-cms/issues/119) |
| Los presupuestos de §8 no se miden                             | Se cumple la estructura; una estructura correcta sin medición es una hipótesis                                                                                                                                                                                                                                                                                   | [#117](https://github.com/KthArg/uno-cms/issues/117) |
| La posición del cursor del editor no se prueba con componentes | jsdom no maqueta: todo lo tecleado entra al principio del documento, con y sin el código que se quería probar. **Sí lo cubre el e2e del editor**, con un navegador de verdad; lo que se acepta es no tener un test rápido que lo vigile                                                                                                                          | —                                                    |
| Falta la documentación para quien despliega                    | `SETUP.md`, `DEVELOPER.md` y `SECURITY.md`                                                                                                                                                                                                                                                                                                                       | [#118](https://github.com/KthArg/uno-cms/issues/118) |
| La revisión de seguridad transversal                           | Es la primera vez que se puede hacer con el sistema entero construido                                                                                                                                                                                                                                                                                            | [#120](https://github.com/KthArg/uno-cms/issues/120) |

### Después del MVP

| Qué                                                       | Por qué se aplazó                                                                                                                                                                                                                                                                                                                                                                                                                                            | Issue                                                |
| --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------- |
| Un test de `unit`+`ui` falló una vez y no vuelve a fallar | El nombre se perdió porque el comando filtraba la salida de vitest. Descartado el formato —`prettier` no cambió nada— y descartada la dependencia de orden con ocho pasadas, tres barajadas. Sin nombre no hay nada que arreglar; queda abierto porque cerrarlo sería decidir que no pasó                                                                                                                                                                    | [#167](https://github.com/KthArg/uno-cms/issues/167) |
| El almacén local no lo cubre ningún e2e                   | La suite corre con `next start`, o sea `NODE_ENV=production`, donde este almacén está apagado a propósito. Cubrirlo pide un segundo proyecto de Playwright contra `next dev` —el doble de suite para ejercitar un servidor que no se despliega— o meterle una puerta trasera a `usarAlmacenLocal()`, que es justo la función que impide que esto se active donde haría daño. Lo que sí está cubierto son los manejadores contra disco y base de datos reales | [#170](https://github.com/KthArg/uno-cms/issues/170) |

### En la vista previa de una web que vive fuera (la fase actual)

| Qué                                                                                    | Por qué se aplazó                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Issue                                                |
| -------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| La ruta de borradores no tiene límite de peticiones                                    | Quien tenga un token válido puede pedirla en bucle, y cada petición son varias lecturas sin caché (ADR-501). **No es una exposición nueva**: `/preview` tiene la misma forma desde M5 y con un token de dos horas en vez de quince minutos. Ponerle un límite solo a la ruta nueva dejaría la vieja igual y daría la sensación de que la superficie está cubierta. Lo que hace falta es una decisión sobre toda la vista previa, no un `if` aquí                                                                                                          | —                                                    |
| Nadie ha comprobado que un iframe a `http://localhost` cargue desde una página `https` | Es el caso «CMS desplegado, web en local» de la spec 08 §1. La CSP lo permite si está en `PREVIEW_ORIGINS` y el panel lo apunta; lo que no se sabe es si el navegador lo bloquea después por contenido mixto, que tiene reglas propias. **No se puede comprobar en local**: hace falta un origen `https` de verdad, y aquí todo corre en `http`. Va con el primer despliegue, igual que las capturas de `SETUP.md`                                                                                                                                        | [#157](https://github.com/KthArg/uno-cms/issues/157) |
| Un objeto que llegue al almacén y no se anote se queda huérfano, y nadie lo sabe       | ADR-705 deja **dos** escrituras y las dos pueden fallar: si el navegador se cierra entre la subida y el registro, y además se pierde el aviso de Vercel, el fichero existe y el CMS no lo tiene. Antes de ADR-705 bastaba con que fallara una. Lo que lo cierra de verdad es reconciliar el almacén con la base cada tanto, y eso son permisos de listado, un trabajo periódico y una decisión sobre qué hacer con lo huérfano — más de lo que este arreglo puede llevar dentro. Hay tres objetos así ahora mismo, del depurado de este mismo camino      | [#206](https://github.com/KthArg/uno-cms/issues/206) |
| El camino que se despliega solo lo ejercita una suite que hay que lanzar a mano        | `pnpm test:humo` cubre desde fuera lo que ninguna otra toca —Vercel Blob, Neon, la CSP aplicada— y está ejecutada en verde contra el despliegue. Lo que queda vivo es que **no corre sola**: hace falta un despliegue de pruebas separado del de verdad y credenciales en el repositorio, y hoy no hay ni lo uno ni lo otro (el repositorio es público desde el 31 de agosto de 2026 y CI vuelve a correr, así que lo que falta es el despliegue de pruebas y sus credenciales). Una suite que hay que acordarse de lanzar protege menos de lo que parece | [#207](https://github.com/KthArg/uno-cms/issues/207) |

---

### En el despliegue y el recorrido a mano

| Qué                                                                                    | Por qué se aplazó                                                                                                                                                                                                                                                                                                                                                                                  | Issue                                                |
| -------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| El arreglo de la etiqueta «Todavía no ha entrado» no repara los despliegues existentes | `completeSetup` ya crea al dueño estrenado, pero las filas escritas antes siguen con el valor viejo. No se arregla con una migración de una línea porque **el dato que haría falta no existe**: `password_version = 0` significa a la vez «invitación sin canjear» y «dueño anterior al arreglo», que es la ambigüedad que causó el fallo. Se arregla solo en cuanto el dueño cambie su contraseña | [#217](https://github.com/KthArg/uno-cms/issues/217) |
| Publicar no purga la caché de `/api/content/:key`                                      | Son 60 s de `s-maxage` con `stale-while-revalidate=300`, fijados en `SPEC.md` §5.3 por #82. La landing propia no se entera porque la pinta el servidor; **una web remota (ADR-701) sí**, y se midió `Age: 71` — por encima del presupuesto de 60 s de §11.2. Purgar al publicar es una decisión sobre toda la caché pública, no un `if` en la ruta                                                 | —                                                    |
| Un identificador real del almacén de Blob vive en un test, y el repositorio es público | `tests/integration/registrar-imagen.test.ts` usa el subdominio real como constante. **No es un secreto** —va en la URL de cada imagen que sirve la web pública— pero usar el identificador de una cuenta concreta como fixture se propaga copiando. Cambiarlo por uno inventado no toca lo que el test prueba                                                                                      | —                                                    |
| La web no tiene favicon                                                                | Cada carga del panel y de la landing produce un 404 de `/favicon.ico`. Es ruido en los registros y un hueco en la pestaña del navegador. No se arregló al encontrarlo porque estaba en mitad de otra cosa y un icono es una decisión de identidad, no un parche                                                                                                                                    | —                                                    |
| El formulario de cambiar contraseña no lleva campo de usuario oculto                   | El propio navegador lo avisa por consola. Sin él, los gestores de contraseñas no asocian bien el cambio y pueden guardar una entrada suelta. Son tres líneas; se anota en vez de colarlo en un PR de otra cosa                                                                                                                                                                                     | —                                                    |
| La suite e2e en paralelo falla en `historial.spec.ts` T-E-3 desde #220                 | Con un worker —como corre CI— pasan los 71, siempre; en paralelo y en local falla ese caso en las cuatro pasadas que se hicieron. Están descartadas la caché en disco de Next y que los casos nuevos toquen su estado, y **el mecanismo sigue sin identificarse**: se abre en vez de cerrarlo con un razonamiento, que es la lección de #134. A diferencia de #167, este sí se reproduce           | [#227](https://github.com/KthArg/uno-cms/issues/227) |
| La landing pública no tiene modo oscuro, y es una exclusión declarada                  | La fase de estética se acotó al panel a propósito: la landing tiene presupuesto de 60 KB de JavaScript y listón de Lighthouse propios. La exclusión vive en `tests/support/panel.ts`, no en la cabeza de nadie — si algún día entra, se quita de allí y los casos que la cubren aparecen solos                                                                                                     | —                                                    |
| La barra de desplazamiento de la ventana no acompaña al modo oscuro                    | El navegador la pinta desde la raíz, y el atributo de tema se pone en el contenedor del panel para no teñir la landing. Es cosmético y la alternativa —`color-scheme` en el `<html>`— le pondría barras oscuras a una página clara                                                                                                                                                                 | —                                                    |

### En el acceso con Google (la fase actual)

| Qué                                                                                         | Por qué se aplazó                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Issue                                                |
| ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| Nadie ha entrado nunca con una cuenta de Google de verdad                                   | Lo comprobado llega hasta donde se puede sin una: las tres puertas contra Postgres real, la identidad que acaba en el token, y que el navegador sale hacia `accounts.google.com` con nuestro identificador. **La vuelta no la toca ningún test**: que el `id_token` se valide contra las claves de Google y que `email_verified` llegue con el valor que Google dice lo hace `@auth/core`, y damos por bueno que lo hace bien. Cerrarlo no es escribir un test: es hacerlo una vez a mano con un cliente de OAuth real y anotar el resultado | [#237](https://github.com/KthArg/uno-cms/issues/237) |
| La suite de e2e no ejercita el estado **sin** Google                                        | Arranca con las dos variables puestas porque es la única forma de probar el viaje en un navegador (T-233-18), y el servidor es uno para toda la suite. La rama apagada la cubren T-233-2 —sobre la lista de proveedores, que es la puerta de verdad— y T-233-15. Es una asimetría declarada en `playwright.config.ts`, no un olvido                                                                                                                                                                                                          | —                                                    |
| Cambiar el correo en el panel cambia con qué cuenta de Google se entra, y no lo avisa nadie | La correspondencia es por correo y no hay nada guardado del proveedor (no hay tabla de cuentas vinculadas: la sesión es JWT y no hay adaptador). El comportamiento es el esperable y está en la spec 13 §9; lo que falta es un aviso en la pantalla de cuenta, que es trabajo de interfaz y no de esta pieza                                                                                                                                                                                                                                 | —                                                    |

---

## Deuda aceptada

Cosas que **no** se van a arreglar, con la decisión escrita. Están aquí para que nadie las
redescubra como si fueran un hallazgo.

| Qué                                                                       | Por qué se acepta                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Dos creaciones simultáneas en una colección pueden empatar en `sortOrder` | Cubrirlo exige un bloqueo consultivo o `SERIALIZABLE`. El daño es que dos elementos creados en el mismo milisegundo salgan en el orden del desempate por clave; el orden sigue siendo **determinista** y el editor los arrastra. Probé el `FOR UPDATE` que parecía arreglarlo y **no arregla nada**: bloquea filas existentes y no protege de una fila que otra transacción inserta                                                               |
| `revertDraft` no lleva `version`                                          | La entrada la fija `SPEC.md` §5.3 y la operación es destructiva por definición. Lo que falta no es un chequeo en la action, es la confirmación en la interfaz — que sí está planificada                                                                                                                                                                                                                                                           |
| `trustHost: true` en Auth.js                                              | `SPEC.md` §0 pide auto-hospedable, así que hay que confiar en el `Host`. Las mitigaciones están en ADR de M2 y en `.env.example`; lo que queda vivo se cierra definiendo `AUTH_URL` en el despliegue                                                                                                                                                                                                                                              |
| Las claves de colección son UUID y no nanoid                              | `SPEC.md` §5.3 dice nanoid; la propiedad que hace falta la da `crypto.randomUUID()` sin una dependencia de tiempo de ejecución (ADR-408). La cadena no la lee nadie                                                                                                                                                                                                                                                                               |
| El tamaño de las imágenes se guarda como 0                                | El callback de subida completada de Blob no trae el tamaño del fichero, y desde ADR-705 la fila la escribe el navegador, que **sí** lo sabe — y por eso mismo tampoco se le pide: sería un dato suyo sin contrastar en una fila que se enseña. Es un dato de presentación: un cero visible es mejor que un número declarado por el cliente que parece medido                                                                                      | —                                                    |
| El límite de tamaño lo declara el cliente **en el camino de Vercel**      | La decisión ocurre antes de que el fichero exista, porque ADR-005 evita que pase por nuestro servidor. El suelo real es el límite por fichero de Vercel Blob; el nuestro adelanta el rechazo y lo explica en español. El almacén local de ADR-700 **sí** lo mide de verdad, y eso no salda esta deuda: lo que se despliega es el otro camino                                                                                                      | —                                                    |
| La condición de concurrencia del canje de invitación no tiene test propio | `redeemInvitation` condiciona su `update` a la versión leída al comprobar el enlace, para que dos canjes solapados no se pisen. No se puede forzar el solapamiento desde fuera sin abrir una costura en el módulo solo para el test. Lo que sí está probado es el contrato —de dos canjes a la vez gana uno y la cuenta acaba con una sola contraseña—, que es lo que se promete hacia fuera                                                      |
| La lista de personas no pagina                                            | Mismo caso que las colecciones: `SPEC.md` describe el equipo de una landing, que son unas pocas cuentas. Paginar ahora sería resolver un problema que este producto no tiene                                                                                                                                                                                                                                                                      |
| «Todavía no ha entrado» se deduce de `password_version`                   | No hay columna de último acceso —`SPEC.md` §4 no la contempla— y añadirla obligaría a escribir en la base de datos en cada acceso. Quien canjea su invitación y nunca llega a entrar aparece como que sí. El dato es una ayuda para saber a quién falta mandarle su enlace, no un registro de actividad                                                                                                                                           |
| La suite e2e corre con un solo worker en CI y en paralelo en local        | Es la asimetría al revés de la habitual: una carrera entre ficheros aparece en la máquina de quien desarrolla y **no** en CI. Se acepta porque el runner tiene dos núcleos y darle cuatro navegadores lo haría más lento y más inestable, no más estricto. La contrapartida es que la ejecución local es la exigente, y eso hay que saberlo: si la suite falla en local y pasa en CI, el sospechoso es el estado compartido, no la máquina (#134) |
| Las construcciones de vista previa migran la base de producción           | ADR-702 aplica las migraciones al construir, y Vercel da a las vistas previas las mismas variables que a producción salvo que se configure otra cosa. Así que una rama con una migración nueva la aplica a la base de producción **al construirse**, antes de que nadie la apruebe. Se resuelve dando a las vistas previas su propia base de datos, que es configuración del despliegue y no código: va con el primer despliegue de verdad        | [#157](https://github.com/KthArg/uno-cms/issues/157) |
| La landing es dinámica y §8 pide ISR                                      | Medido en ADR-502: 6,8 ms contra 3,6 ms de mediana, sobre un presupuesto de LCP de 2500 ms. Lo que compra la versión estática es que `pnpm build` exija una base de datos accesible, y §0 exige auto-hospedable. Volver es quitar una línea, y está dicho en el fichero                                                                                                                                                                           |
| El esquema laxo no se valida en el navegador                              | Replicarlo exigiría llevar `config`, `schema-gen` y `richtext` al cliente, o sea mover la frontera de `server-only` entera. Lo que se pierde es cazar nuestros propios errores —un campo mal escrito llega y se ignora en silencio—, no una vía de ataque                                                                                                                                                                                         |
| El editor guarda al pasar de un campo a otro                              | SPEC §8 habla del "blur del formulario" y el `onBlur` del contenedor salta también al tabular entre campos. Guardar de más es el lado correcto en el que equivocarse; la alternativa —comparar `relatedTarget`— es más código y más frágil por una escritura barata                                                                                                                                                                               |

---

## Contradicciones de la spec ya resueltas

Cada una con su ADR. Se listan porque quien lea `SPEC.md` de cero se va a topar con ellas.

| Contradicción                                                                           | Resolución                                            |
| --------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| §5.2 aplica el esquema estricto a valores vacíos, y §5.1 pide "valores vacíos/default"  | ADR-404: leer no lanza nunca y resuelve campo a campo |
| §9 exige el nombre de la sección en los avisos; §5.1 no le da nombre a ningún singleton | ADR-406: `s.object` acepta etiqueta                   |
| §5.3 pide `deactivateUser`; §4 no tiene dónde guardarlo                                 | ADR-409: columna `active`, con sus tres consecuencias |
| ADR-002 fija un driver HTTP; §4 exige transacciones                                     | ADR-200: driver WebSocket                             |
| §6.3 pide sanear richtext; §7.1 lo prohíbe en cliente                                   | ADR-107 y #19, pendiente de verificación en M5        |
